---
spec-version: v2
feature: capability-auto-complete
---

# Plan: capability-auto-complete

**The adapter lands first, alone, and every prose task calls what it shipped.** All capability writing lives in one new `scripts/gspec-backlog.sh` subcommand. That script is the only place this plugin reads or writes `gspec/` (ADR 0020). The three loop sites and the adopt path are then prose that pipes its output. A prose task written before T1 would describe a command that does not exist yet. That is why every other task depends on T1.

**The subcommand is `complete-capabilities <slug> [root]`, and it settles the PRD's deferred naming.** It prints one `COMPLETED=<slug>\t<capability text>` line per box it flips. That is deliberately the same two-field shape as `capability-drift`'s `DRIFT=` line, so the kickoff and the stop report render a flip with the line form they already use for a drift row. As shipped (T1), it opens with the summary line `COMPLETE_CAPABILITIES=<ok|blocked|none> completed=<n>`, prints `FILE=<relative prd path>` whenever the feature resolved (even at `completed=0`), and adds a `REASON=` line for `blocked`, `none` and unresolved slugs. `completed=<n>` with `n` greater than 0 is the signal a caller stages on (T7 aligns §3.6 to it). `blocked` is the feature-wide hold for an unchecked task's unmatched quote.

Exit codes mirror `check-task`:
- 0 for the non-gspec skip;
- 1 for a missing or malformed slug;
- 4 for a slug that resolves to no PRD in any layout. This is the distinguishable failure.

A PRD with no plan file is not a failure. It has no covering task, so the call exits 0 with `COMPLETE_CAPABILITIES=ok completed=0`.

**The flip rule reuses the drift derivation, and it is narrower than the drift report in one place.** A capability flips only when it is unchecked, canonically shaped, covered by at least one task, and every task covering it is checked. That is exactly `_capability_drift_for`'s `DRIFT=` condition, reused rather than copied. There is one extra hold, and it is feature-wide: any unchecked task whose `covers:` quote matches nothing flips no capability at all. The drift scan reports `unmatched-quote` for checked tasks as well, but a checked task with a bad quote is frozen history and cannot hide evidence against a flip. So only unchecked tasks hold the feature, and the sweep proves that distinction in both directions. Any refactor that exposes the task's checked bit must leave `capability-drift`'s output byte-identical.

**`capability-drift` stays read-only, and `check-task` stays one id, one line.** The detector gains no flag, mode or side effect. Its existing checksum case is not edited. The new subcommand is a second, separately named write that sits beside `check-task` and does not widen it. The only bytes it changes are the `[ ]` of a flipped capability line. It uses `check-task`'s two passes: a read-only lookup, then a `sub()` against the untouched `$0`. The file is never opened for writing unless a real flip is about to happen.

**Driver mode never has to be relaxed for this.** ADR 0028 refuses the driving session's own main-thread edits outside `.agents/`. It allows git and gaffer's own scripts. So every flip is done by the adapter, and every commit is done by `git`. No site edits a PRD with `Edit` or `sed`.

**Reconciliation commits share one message form and name their site.** This settles the PRD's second deferred decision. The form is `spec: reconcile capability record (preflight|end-of-run|adopt)`. It carries no `[orch packet:]` or `[orch decider:]` trailer, so `metrics.sh` counts no packet for it and the preflight trailer scan never reads it as landed work. It stages only the PRDs whose `FILE=` lines the flips named. If the commit is refused or fails, the site reports that. It then restores those PRDs with `git checkout -- <files>` so the flipped boxes never ride into a later packet uncommitted, and it never halts.

**Only capability drift changes hands.** `skills/run-loop/SKILL.md` §1 has two preflight scans. The trailer-versus-task scan keeps its report-only rule and its "reconciling a drifted record is the human's call" sentence: this PRD reverses the capability decision only. The capability bullet currently borrows that authority with "on the same authority as the scan above". T3 rewrites it to stand on its own, so the two bullets no longer share a justification that is now true of only one of them.

**End-of-run reconciliation follows the work, not the PRD's literal branch.** The PRD says reconciliation commits land on the integration branch. At end of run below `full-autonomy` the run's work has not been merged there, so a flip committed there would mark capabilities complete ahead of their work. T3 commits on the run branch in that case. Approved by the operator 2026-09-17.

**Every script task names its sweep case.** T1 names `scripts/test-gspec-backlog.sh`. T2 to T5 are prose that is read at dispatch or never loaded. No sweep can reach them, so each task names the concrete read a reviewer performs instead.

**File contention, and why four tasks are `[P]`.**
- T1 is the only task that touches `scripts/gspec-backlog.sh` and `scripts/test-gspec-backlog.sh`.
- `skills/run-loop/SKILL.md` belongs to T2 and T3, split by site (§3.6 versus §1 and §4). T3 collides with T2, so it does not take the marker.
- `skills/resume/SKILL.md` is T4's alone.
- `CLAUDE.md`, `docs/adr/0025-remove-backlog-done.md` and `gspec/features/completion-record-drift/prd.md` are T5's alone.

T1, T2, T4 and T5 hold file sets disjoint from each other, and so do T6 (the adapter and its sweep, after T1 shipped), T7 (`run-loop`) and T8 (`resume` and the ADR, after T5 shipped). Every dep points strictly backwards in the plan order.

**`gspec-adapter-consistency` must not be in flight against T1 or T6.** It refactors this adapter's shared pattern blocks and adds cases to the same sweep. There is no logical dependency either way, but the two edit the same two files. It has no plan today.

**Reflexivity.** `scripts/*.sh` take effect mid-run, so the moment T1 lands, a later task in the same run can call `complete-capabilities`. `skills/run-loop/SKILL.md` and `skills/resume/SKILL.md` are read when the command is invoked, so the run that lands T2 to T4 is still driving under the old prose. That run's own end-of-run scan will therefore report this feature's capabilities rather than flip them. The next `/gaffer:run-loop` preflight reconciles them: that is the first run under the new contract. No task touches `hooks/hooks.json`, a settings file that registers hooks, or any agent's or skill's FRONTMATTER, so no packet here owes a `session_boundary` declaration.

Every regression sweep must pass green after every task.

## Plan

- [x] **T1** [P] **P0** Add `complete-capabilities <slug> [root]` to `scripts/gspec-backlog.sh`, with the output contract, exit codes and flip rule stated above; register it in the header Subcommands list and the usage line. Reuse `_capability_drift_for`'s derivation (`_prd_capabilities`, `_plan_task_covers`, `_split_covers`, `_prd_capability`) instead of re-deriving it. Hold every flip in a feature with an unchecked task whose quote is unmatched, and never flip an uncovered or unrecognized capability. Never unflip a checked one, even with an unchecked covering task. Write with `check-task`'s read-only-lookup-then-`sub()` shape, so only the flipped lines' checkbox characters change. Leave `capability-drift`'s output byte-identical and its existing checksum case untouched. Add cases to `scripts/test-gspec-backlog.sh`, with fixtures in both the flat and feature-folder layouts: the two-capability fixture (the all-checked capability is flipped and named; the one with an unchecked covering task is untouched and absent); one case per unjudgeable class, each asserting no box flipped; the unmatched-quote case (capability A's matched tasks all checked, plus an unchecked task with an unmatched quote; A stays unchecked); its converse (a CHECKED task with an unmatched quote does not hold A's flip); a checked capability with an unchecked covering task stays checked; reverting the flipped lines yields a PRD byte-identical to the original, by checksum; a second run names nothing and leaves the file byte-identical to the first run's result; `capability-drift` over the same fixtures is byte-identical before and after the subcommand exists; and the non-gspec skip, the malformed slug and the unresolvable slug, each asserting its exit code and summary line.
  - deps: —
  - covers: The adapter can mark a feature's finished capabilities complete · The drift detector itself stays read-only
  - arch: —
  - files: scripts/gspec-backlog.sh, scripts/test-gspec-backlog.sh
- [x] **T2** [P] **P0** Extend `skills/run-loop/SKILL.md` §3.6 so that, after the per-member `check-task` loop and before the commit, the loop runs `gspec-backlog.sh complete-capabilities <slug>` once for the landed feature. The slug comes from the members' `CHECKED=<feature>#T<n>` lines; a bundle is always one feature. The step is skipped when every member returned `CHECKED=none`. The PRD is staged into the one packet commit only when a `FILE=` line is printed; a packet that completes no capability leaves its PRD out. Exit 4, exit 1 or any other failure is reported on the existing shape-A packet line, naming the feature, and restores the PRD with `git checkout --` when no `FILE=` line was printed; it never halts, never records an outcome other than `green`, and never withholds the commit — unlike `check-task`'s exit-1 rule, which stays as it is, and the task record is unaffected. A single-task packet with no capability completed reads exactly as it does today. Prose only, no sweep case; checkable by reading §3.6 for the call's position (after every member's flip, before the commit), for `FILE=`-gated staging, and for a failure path that lands the packet.
  - deps: T1
  - covers: A capability completes in the same commit as the task that completes it
  - arch: —
  - files: skills/run-loop/SKILL.md
- [x] **T3** **P0** Rewrite `skills/run-loop/SKILL.md` §1's capability-drift bullet and §4's end-of-run rescan so each site reconciles instead of only reporting: for every slug named on a `DRIFT=` line, call `complete-capabilities <slug>` through the adapter, never as a main-thread edit, and land that scan's flips as one commit in the reconciliation form above. At preflight the commit goes on the integration branch — if the checkout is not on it, report the flips and restore the PRDs rather than commit; at end of run it goes on the integration branch after §3.7's merge at `full-autonomy`, and below `full-autonomy` on the run branch, so a flip never reaches the integration branch ahead of the work it records. The kickoff names every flip by feature and capability in the existing per-row ⚠️ line form; the stop report's `▶ Next` section names every flip as an unglyphed line; no new glyph, shape or tally figure. `unjudgeable=<n>` is still stated at both sites with its classes, and is never flipped. A failed flip or commit is reported, restores the touched PRDs, and never halts. `CAPABILITY_DRIFT=none` stays a silent no-op. Replace "reconciling a drifted record is the human's call" in the capability bullet with the loop reconciling judgeable drift and reporting unjudgeable rows to the operator, and leave the trailer-versus-task bullet's report-only rule and wording untouched. Prose only, no sweep case; checkable by reading §1 and §4 against T1's output contract and by confirming the task-drift bullet is unchanged.
  - deps: T1, T2
  - covers: Drift the loop finds is reconciled, not handed to the operator · The standing wording says the loop reconciles the record
  - arch: —
  - files: skills/run-loop/SKILL.md
- [x] **T4** [P] **P0** Extend `skills/resume/SKILL.md`'s `adopt` path so that, after the per-member `check-task` flips, it runs `complete-capabilities <slug>` for the adopted feature. The flips go into the existing follow-up commit when one is made, and otherwise into their own commit; either way that commit — covering the task flips and the capability flips together when combined — uses the reconciliation form with site `adopt`, is never an amend of the orphan commit, and carries no packet trailer. A failure is reported without halting and without changing the `green` attestation, so a crash between landing and recording leaves no capability drift. State in §1's gspec preflight that resume runs no capability-drift scan of its own — the resumed run reconciles at `run-loop` §4's end-of-run scan — so no second phrasing of the rule exists, and confirm nothing in the file still assigns capability reconciliation to the human. Prose only, no sweep case; checkable by reading the adopt path against `skills/run-loop/SKILL.md` §3.6 and §1 for one call, one commit form and one failure rule.
  - deps: T1, T3
  - covers: A capability completes in the same commit as the task that completes it · The standing wording says the loop reconciles the record
  - arch: —
  - files: skills/resume/SKILL.md
- [x] **T5** [P] **P1** Update the standing wording in three files. In `CLAUDE.md`'s ADR 0024 + ADR 0025 bullet, `check-task` is no longer "the plugin's ONLY write into `gspec/`": name `complete-capabilities` as the second, separately named write (only the checkbox characters of capability lines, never unflipping), and state that the loop reconciles judgeable capability drift at preflight, at land, on adopt and at end of run, reporting unjudgeable rows to the operator. In `docs/adr/0025-remove-backlog-done.md`, append a dated `Revision 2026-09-17` section that extends D1's in-commit flip to capability boxes and records the reversal of `completion-record-drift`'s "Flipping a capability checkbox, automatically, ever" as history, quoting that reasoning and marking it reversed, with this feature's justification; D1's "one write into `gspec/`" sentence stays and is not deleted. In `gspec/features/completion-record-drift/prd.md`, add a one-line reversed-by pointer to this feature beside the parent's `Out` bullet, in free prose outside every capability, so its completion is unchanged. Prose only, no sweep case; checkable by reading the three edits for one consistent statement of the new rule, and by confirming every piece of the old reasoning is still present, dated and marked reversed.
  - deps: T3, T4
  - covers: The standing wording says the loop reconciles the record
  - arch: —
  - files: CLAUDE.md, docs/adr/0025-remove-backlog-done.md, gspec/features/completion-record-drift/prd.md
- [x] **T6** [P] **P0** Make `complete-capabilities` in `scripts/gspec-backlog.sh` handle a PRD with no trailing newline, and prove it in `scripts/test-gspec-backlog.sh` with a fixture whose final line is a flippable capability line with no trailing newline. The case asserts that the capability is flipped and named, that the file still lacks a final newline, and that reverting the flipped line gives a PRD byte-identical to the original by checksum. Model it on the existing `check-task` no-trailing-newline case. The subcommand already carries a `tail -c1` guard, so fix the subcommand only if the new case fails, and leave `capability-drift`'s output and its checksum case untouched.
  - deps: —
  - covers: Follow-up correctness fixes from the end-of-run review
  - arch: —
  - files: scripts/gspec-backlog.sh, scripts/test-gspec-backlog.sh
- [x] **T7** [P] **P0** Amend `skills/run-loop/SKILL.md` in three sites. First, §1's capability-drift bullet: name each slug whose `complete-capabilities` call returns `COMPLETE_CAPABILITIES=blocked` once in the kickoff, using the existing per-row ⚠️ line form, with that call's `REASON=` text. Second, §4's end-of-run rescan: name each such slug once in the stop report's `▶ Next` section on the backlog-complete path, as an unglyphed line with the same reason. At both sites, state that nothing is flipped, restored or committed for a held feature, and that it is neither a failure nor a flip. Third, §3.6's capability step: stage the PRD only when the summary reads `completed=<n>` with `n` greater than 0, which is the same test §1 and §4 already use, instead of whenever `FILE=` is printed. Rewrite the "`CHECKED=none` — the non-gspec case" skip so it skips only when every member returned `CHECKED=none` at exit 0, because an exit-4 drift member also prints `CHECKED=none` and takes the handoff's `FEATURE=` fallback. Give the failure restore's reason for using `git checkout -- <path>`, which restores from the index, rather than `HEAD`: the index holds anything the packet itself staged, and a HEAD restore would discard it — confirm this reasoning against the actual staging order before writing it down. Prose only, no sweep case. A reviewer checks it by reading §1 and §4 for one held-feature line each, with the adapter's reason and no flip, restore or commit, and by reading §3.6 for the `completed=<n>` staging test, the exit-0-only skip and the stated restore reason.
  - deps: —
  - covers: A feature the loop cannot complete yet is still named · Follow-up correctness fixes from the end-of-run review
  - arch: —
  - files: skills/run-loop/SKILL.md
- [ ] **T8** [P] **P0** Bring `skills/resume/SKILL.md`'s adopt path and `docs/adr/0025-remove-backlog-done.md`'s `Revision 2026-09-17` into line with T7. In the adopt path, when no member printed a `CHECKED=<feature>#T<n>` line and `$RUN_DIR/<cursor>/handoff.md` does not exist, skip capability completion entirely, with no call, no restore and no commit, and state that the resumed run's `run-loop` §4 end-of-run scan reconciles it. Describe staging the PRD in the same `completed=<n>` terms as §3.6. Apply the same exit-0-only reading to the "`CHECKED=none` — the non-gspec case" skip. Describe a `blocked` result as a held feature: named with the adapter's `REASON=`, never flipped, and with nothing restored or committed. In the ADR revision, add that same held-feature description beside its unjudgeable-rows bullet: named at the kickoff and at the stop report's next steps, never flipped, with the subcommand's reason. Prose only, no sweep case. A reviewer checks it by reading the adopt path against `skills/run-loop/SKILL.md` §3.6 for identical staging and skip wording and a missing-handoff skip, and by reading the three files for one wording of the held-feature rule.
  - deps: T7
  - covers: A feature the loop cannot complete yet is still named · Follow-up correctness fixes from the end-of-run review
  - arch: —
  - files: skills/resume/SKILL.md, docs/adr/0025-remove-backlog-done.md
