# UX references & design decisions — {{PROJECT_NAME}}

> Durable design-research log for the `ux-designer` agent. It reads this first on
> each UI task and appends to it — the design counterpart to an ADR. Keep entries
> short and sourced; adapt patterns, never copy assets or trademarked styling.
> Delete this file if the project has no user-facing surface.

## Design language

_Point at the authoritative sources so the ux-designer aligns with them rather
than inventing a parallel style:_

- Style / tokens: `gspec/style.md` (or `gspec/style.html`) — fill in once it exists.
- Component library / framework: _e.g. the design system in use, if any._
- Supported breakpoints & themes: _e.g. mobile / tablet / desktop; light / dark._

## Comparable products worth studying

_Products that solve a similar problem well, to research before designing a new
surface. Add as you find them._

| Product | What it does well | Where seen |
| --- | --- | --- |
| _e.g. competitor / gallery_ | _pattern to learn from_ | _URL / screenshot_ |

## Decisions log

_Append one entry per non-trivial design decision: the surface, the pattern
chosen, the comparable product(s) that informed it, and why it fits here._

<!--
### {{DATE}} — <surface / component>
- **Pattern:** <what was chosen>
- **Informed by:** <comparable product(s) + where seen>
- **Why here:** <fit with this product's language / users / constraints>
- **Result:** <files touched, screenshot ref>
-->
